{% extends 'baseAdmin.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}
     {% block content %}
    <h1>Comment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ comment.content }}</td>
            </tr>
            <tr>
                <th>Nblikes</th>
                <td>{{ comment.nblikes }}</td>
            </tr>
            <tr>
                <th>Nblikes</th>
                <td>{{ comment.post |raw}}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary mr-1 mb-1" href="{{ path('comment_index') }}">back to list</a>

    <a class="btn btn-outline-warning mr-1 mb-1" href="{{ path('comment_edit', {'id': comment.id}) }}">edit</a>

    {{ include('comment/_delete_form.html.twig') }}
{% endblock %}
 {% endblock %}
