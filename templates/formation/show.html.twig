{% extends 'base.html.twig' %}

{% block title %}Formation{% endblock %}

{% block body %}
<div class="app-content content">
        <div class="content-overlay"></div>
        <div class="header-navbar-shadow"></div>
        <div class="content-wrapper">


    <h1>Formation</h1>
 <div class="card">
                                <div class="card-content">
                                    <img src="/uploads/{{formation.getImg()}}" alt="Card image">
                                    <div class="card-body">
                                        <h5>{{ formation.Id }}</h5>
                                         <p class="card-text  mb-0">{{ formation.nomformation }}</p>
                                        <p class="card-text  mb-0">{{ formation.nomdeformateur }}</p>
                                        <span class="card-text">{{ formation.sujetdeformation }}</span>
                                        <div class="card-btns d-flex justify-content-between mt-2">
                                            <a href="#" class="btn gradient-light-primary text-white">Follow</a>
                                         

                                        </div>
                                    </div>
                                </div>
                        

    <a href="{{ path('formation_index') }}">back to list</a>

    <a href="{{ path('formation_edit', {'id': formation.id}) }}">edit</a>

    {{ include('formation/_delete_form.html.twig') }}

    
    </div>
{% endblock %}
