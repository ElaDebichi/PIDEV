
    {{ form_start(form, {'attr':{ 'class':'main-contact-form wow'}}) }}

                {{ form_widget(form.libelle, {'attr':{ 'class':'form-control', 'placeholder':'libelle *'}}) }}
                <button class="btn btn-primary btn-black">{{ button_label|default('Save') }}</button>

    {{ form_end(form) }}


