{% extends 'baseFront.html.twig' %}

{% block title %}Edit Comment{% endblock %}

{% block body %}

        <div class="app-content content">
        <div class="content-wrapper">
        <div class="content-body">
            <section id="basic-vertical-layouts">
                <div class="row match-height">
                    <div class="col-md-6 col-12">
                        <div class="card">
                            <div class="card-header">
                                <h4 class="content-header-title float-left mb-0">Edit Comment</h4>
                            </div>

                    <div style="text-align: right;">
                        {{ include('user_post/_deleteComment_form.html.twig') }}
                        {{ include('user_post/_formcomment.html.twig', {'button_label': 'Update'}) }}

                        <a class="btn btn-flat-primary mr-1 mb-1" href="{{ path('candidat_showFront',{'id':app.user.id}) }}">back to list</a>

                    </div>
                        </div>
                    </div>
            </section>
        </div>
        </div>
        </div>
{% endblock %}