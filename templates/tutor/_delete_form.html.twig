<form method="post" action="{{ path('tutor_delete', {'id': tutor.id,'iduser':employer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tutor.id) }}">
    <button class="btn btn-flat-danger mr-1 mb-1"><i class="feather icon-trash-2"></i></button>
</form>
